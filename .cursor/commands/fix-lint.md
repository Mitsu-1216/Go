---
description: リンタ・フォーマッタの指摘を直す
---

## 目的

プロジェクトで使っているリンタ・フォーマッタの警告・エラーを解消する。

## 手順

1. リポジトリのルールに従い、`go vet` / `golangci-lint` / `go fmt` など、プロジェクトで定義されているコマンドを確認して実行する。
2. 指摘ごとに原因を特定し、動作を変えない範囲で修正する。
3. 修正後に同じチェックを再実行し、エラーが残っていないことを確認する。
4. 設定変更が必要な場合は、その理由を短く説明する。
